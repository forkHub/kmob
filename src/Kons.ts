namespace ha.kmob {
    export class Kons {
        readonly HAL_FUNGSI: string = "#fungsi";
        readonly HAL_PILIH_BARIS: string = "#pilih-baris"
        readonly SERVER: string = "http://localhost/kmob/web/"
    }

    // export var kons: Kons = new Kons();
}